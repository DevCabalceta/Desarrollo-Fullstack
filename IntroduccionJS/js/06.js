// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1); // Redondea hacia arriba
resultado = Math.floor(2.9); // Redondea hacia abajo
resultado = Math.sqrt(144); // Raiz cuadrada
resultado = Math.abs(-200); // Valor absoluto
resultado = Math.min(3, 5, 1, 8, 2, 10); // Valor minimo
resultado = Math.max(3, 5, 1, 8, 2, 10); // Valor maximo
resultado = Math.random(); // Numero aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 30); // Numero aleatorio entre 0 y 30

console.log(resultado);